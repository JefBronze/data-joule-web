# Upstash Redis — get from https://console.upstash.com
# Use SEPARATE databases for production vs preview/development
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
REDIS_URL=

# API key for /api/ingest (Pi → Vercel auth)
# Production: strong random key (keep secret)
# Preview/Development: separate test key
INGEST_API_KEY=
